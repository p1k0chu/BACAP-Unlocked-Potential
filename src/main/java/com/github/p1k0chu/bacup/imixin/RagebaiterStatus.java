package com.github.p1k0chu.bacup.imixin;

import net.minecraft.world.entity.player.Player;

public interface RagebaiterStatus {
    void bacup$setRagebaiter(Player entity, boolean status);
    boolean bacup$isRagebaiter(Player entity);
}
